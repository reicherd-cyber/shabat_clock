export async function tick() {
  // TODO: compute device-local occurrences, process missing rows, retry failed rows,
  // and reconcile once-schedule terminal OFF rows per SPEC 5.4.
  return { processed: 0 };
}

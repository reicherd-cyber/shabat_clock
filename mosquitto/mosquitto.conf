# Local dev broker only. Anonymous access is fine here because the port is not
# exposed beyond localhost. Do NOT use this config in production (SPEC §5 uses
# 8883 TLS with per-device creds + ACLs).
listener 1883
allow_anonymous true
